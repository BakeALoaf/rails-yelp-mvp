<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <a class="nav-link" href="/restaurants">Home</a>
      </li>
      <li class="nav-item active">
        <a class="nav-link disabled" href="<%= new_restaurant_path %>">Add New Restaurant<span class="sr-only">(current)</span></a>
      </li>
    </ul>
  </div>
</nav>
<%= simple_form_for @restaurant do |f| %>
  <%= f.input :name, placeholder: "Restaurant Name here" %>
  <%= f.input :address, placeholder: "Restaurant Address here" %>
<%= f.input :phone_number, placeholder: "Phone number Here" %>
  <%= f.input :category, placeholder: "Write the category here (Chinese, Italian, Japanese, French and Belgian)" %>
  <%= f.submit "Add", class: "btn btn-primary" %>
<% end %>
